import styled from "styled-components";

export default styled.div`
    label {
        .label-text {
            color: #111;
            font-weight: 400;
            line-height: 28px;
        }
    }
`